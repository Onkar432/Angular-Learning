Functions:

We first created component name as component

then add html accordingly now 
we can send parameter to function as
----------------------------------------------------------------------------------------------------

  <h1>Welcome to function</h1>
<button (click)="getName('Onkar')">Click Me</button>

----------------------------------------------------------------------------------------------------
function is created inside components ts file
import { Component } from '@angular/core';

@Component({
  selector: 'app-component',
  templateUrl: './component.component.html',
  styleUrls: ['./component.component.css']
})
export class ComponentComponent {
  getName(Name: string) {               -->Created function
    alert(`Welcome ${Name}`)
  }

}
----------------------------------------------------------------------------------------------------
